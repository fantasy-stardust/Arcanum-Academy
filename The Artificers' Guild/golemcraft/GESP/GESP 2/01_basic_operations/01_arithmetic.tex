% !TeX program = xelatex
\documentclass[UTF8,a4paper,12pt]{ctexart}
\usepackage{geometry}
\usepackage{listings}
\usepackage{xcolor}

\geometry{a4paper, top=0.4in, bottom=1in, left=1in, right=1in}

% 铸灵殿配色 — 铁灰框架，琥珀关键词，石青注释
\definecolor{ironframe}{RGB}{72,72,80}
\definecolor{amberkey}{RGB}{180,120,20}
\definecolor{slatecomment}{RGB}{100,115,130}
\definecolor{runestring}{RGB}{160,82,45}

\lstset{
    language=C++,
    basicstyle=\ttfamily\small,
    keywordstyle=\color{amberkey}\bfseries,
    commentstyle=\color{slatecomment}\itshape,
    stringstyle=\color{runestring},
    numbers=left,
    numberstyle=\tiny\color{ironframe},
    rulecolor=\color{ironframe},
    frame=shadowbox,
    rulesepcolor=\color{ironframe},
    breaklines=true
}

\title{四则运算 · 基础铸灵算术\\[4pt]
\large Arithmetic · Foundational Golem Calculus}
\author{铸灵殿 · 二阶学徒卷\\[2pt]
\small Golemcraft Hall · Stage 2 Apprentice Scroll}
\date{}

\begin{document}

\maketitle

\begin{center}
\textit{铸灵术的基础在于精确的数值运算——\\
每一道指令刻入符文石时，必须分毫不差。}
\end{center}

\vspace{0.5em}

\section*{铸灵之基：整数运算 (Integer Operations)}

\begin{lstlisting}
int a = 10, b = 3;
cout << a + b << endl;  // 13（加法）
cout << a - b << endl;  // 7（减法）
cout << a * b << endl;  // 30（乘法）
cout << a / b << endl;  // 3（整数除法）
cout << a % b << endl;  // 1（取余）
\end{lstlisting}

\section*{浮点精炼：浮点数运算 (Floating-point Operations)}

\begin{lstlisting}
double x = 10.5, y = 2.5;
cout << x + y << endl;  // 13.0（加法）
cout << x - y << endl;  // 8.0（减法）
cout << x * y << endl;  // 26.25（乘法）
cout << x / y << endl;  // 4.2（浮点除法）
\end{lstlisting}

\section*{符文词典 (Arcane Glossary)}

\begin{center}
\begin{tabular}{|l|l|p{10cm}|}
\hline
\textbf{英文名词} & \textbf{中文名词} & \textbf{解释} \\
\hline
Integer & 整数 & 不包含小数部分的数字，如 10、-5、0。在铸灵符文中以 \texttt{int} 符记刻录。 \\
\hline
Floating-point & 浮点数 & 包含小数部分的数字，如 10.5、3.14。在铸灵符文中以 \texttt{double} 或 \texttt{float} 符记刻录，用于精密运算。 \\
\hline
Addition & 加法 (+) & 两个数相加，求和——铸灵体汇聚能量的基本操作。 \\
\hline
Subtraction & 减法 (-) & 两个数相减，求差——铸灵体消耗或分离资源时使用。 \\
\hline
Multiplication & 乘法 (*) & 两个数相乘，求积——用于铸灵体的倍增与放大指令。 \\
\hline
Division & 除法 (/) & 两个数相除，求商。整数除法会截断小数部分，如同铸灵体舍弃不完整的碎片。 \\
\hline
Modulo & 取余 (\%) & 求两个整数相除后的余数，仅适用于整数类型。铸灵体常借此判断循环与周期。 \\
\hline
Divisibility & 整除 & 当 \texttt{a \% b == 0} 时，称 a 能被 b 整除——意味着分配完全，无余数残留。 \\
\hline
\end{tabular}
\end{center}

\vfill
\begin{center}
\textit{「若思维能被足够清晰地命名，它便往往能被构建。」\\
--- 铸灵殿殿训}
\end{center}

\end{document}
